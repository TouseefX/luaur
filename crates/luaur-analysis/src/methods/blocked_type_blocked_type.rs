use crate::functions::fresh_index::fresh_index;
use crate::records::blocked_type::BlockedType;

impl BlockedType {
    pub fn blocked_type_blocked_type(&mut self) {
        self.index = fresh_index();
    }
}
