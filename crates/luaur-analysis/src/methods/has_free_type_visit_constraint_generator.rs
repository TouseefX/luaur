//! `HasFreeType::visit(TypeId)` (ConstraintGenerator.cpp:200-205).
//!
//! The canonical implementation lives on the record itself
//! (`crate::records::has_free_type::HasFreeType::visit_type_id`); this module
//! intentionally carries no duplicate `impl` to avoid `E0592` duplicate
//! definitions.
