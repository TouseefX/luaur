//! `HasFreeType::visit(TypePackId)` (ConstraintGenerator.cpp:207-211).
//!
//! The canonical implementation lives on the record itself
//! (`crate::records::has_free_type::HasFreeType::visit_type_pack_id`); this
//! module intentionally carries no duplicate `impl` to avoid `E0592` duplicate
//! definitions.
