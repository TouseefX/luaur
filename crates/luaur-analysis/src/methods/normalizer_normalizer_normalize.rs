//! @interface-stub
use crate::records::normalizer::Normalizer;

impl Normalizer {
    /// @delete
    #[allow(dead_code)]
    pub fn normalizer_normalizer(_other: &Normalizer) {
        unimplemented!("Normalizer copy constructor is deleted in C++");
    }
}
