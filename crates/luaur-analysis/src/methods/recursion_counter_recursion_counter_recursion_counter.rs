use crate::records::recursion_counter::RecursionCounter;

impl RecursionCounter {
    #[allow(dead_code)]
    pub fn recursion_counter_recursion_counter(&mut self) {
        unimplemented!("RecursionCounter copy constructor is deleted in C++");
    }
}
