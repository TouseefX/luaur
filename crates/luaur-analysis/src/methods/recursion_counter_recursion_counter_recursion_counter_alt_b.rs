//! @interface-stub
use crate::records::recursion_counter::RecursionCounter;

impl RecursionCounter {
    #[allow(dead_code)]
    pub fn recursion_counter_recursion_counter_mut(&mut self) {
        unimplemented!("RecursionCounter move constructor is deleted in C++");
    }
}
