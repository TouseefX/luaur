use crate::records::stack_pusher_non_strict_type_checker::StackPusher;

impl StackPusher {
    pub fn stack_pusher_stack_pusher_mut_3(&mut self, _other: &StackPusher) -> Self {
        panic!("StackPusher copy constructor is deleted in C++")
    }
}
