use crate::records::boost_like_reporter::BoostLikeReporter;

pub fn boost_like_reporter_subcase_start(
    _self: &mut BoostLikeReporter,
    _signature: *const core::ffi::c_void,
) {
    // Native-only; doctest subcase_start hook
}
