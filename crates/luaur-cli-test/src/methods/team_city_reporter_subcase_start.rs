use crate::records::team_city_reporter::TeamCityReporter;

pub fn team_city_reporter_subcase_start(
    _self: &mut TeamCityReporter,
    _in: *const core::ffi::c_void,
) {
    // This is an empty override of a doctest reporter method.
}
