use crate::records::boost_like_reporter::BoostLikeReporter;

pub fn boost_like_reporter_boost_like_reporter(boost_like_reporter: &mut BoostLikeReporter) {
    boost_like_reporter.boost_like_reporter();
}
