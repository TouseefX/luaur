use crate::records::boost_like_reporter::BoostLikeReporter;

pub fn boost_like_reporter_test_case_reenter(_this: &mut BoostLikeReporter) {
    // Native-only empty override in C++.
    // Kept as a stub matching the empty body.
}
