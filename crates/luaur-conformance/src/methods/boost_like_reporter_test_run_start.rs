use crate::records::boost_like_reporter::BoostLikeReporter;

pub fn boost_like_reporter_test_run_start(_this: &mut BoostLikeReporter) {
    // Native-only override: `void test_run_start() override {}` (empty)
}
