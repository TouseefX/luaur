use crate::records::team_city_reporter::TeamCityReporter;

pub fn team_city_reporter_subcase_end(_this: &mut TeamCityReporter) {
    // Native-only override: `void subcase_end() override {}` (empty)
}
