use crate::records::team_city_reporter::TeamCityReporter;

pub fn team_city_reporter_test_case_reenter(
    _this: &mut TeamCityReporter,
    _in_data: &core::ffi::c_void,
) {
    // The method is already implemented in the TeamCityReporter record file.
}
