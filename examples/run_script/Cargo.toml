# Example crate: run a Luau script with luaur.
#     cargo run -p luaur-example-run-script
[package]
name = "luaur-example-run-script"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
# In your own project this would be: luaur = "0.1"
luaur = { path = "../../crates/luaur" }
